using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EquipArea : MonoBehaviour
{
   /*[SerializeField] private GameObject rightHand = null;
    [SerializeField] private GameObject leftHand = null;

    public equipAreas equipArea;
    public enum equipAreas
    {
        equipBothHands = 0,
        equipRightHand = 1,
        equipLeftHand = 2,
        equipEitherHand = 3,
        equipNone = 4
    }

    private void OnDrawGizmos()
    {
        switch (equipArea)
        {
            case equipAreas.equipBothHands:
                Gizmos.color = new Color(1, 0, 0, 0.3f);
                Gizmos.DrawCube(rightHand.transform.position, Vector3.Scale(Vector3.one, rightHand.transform.localScale));
                Gizmos.DrawCube(leftHand.transform.position, Vector3.Scale(Vector3.one, leftHand.transform.localScale));
                break;
            case equipAreas.equipRightHand:
                Gizmos.color = new Color(0, 1, 0, 0.3f);
                Gizmos.DrawCube(rightHand.transform.position, Vector3.Scale(Vector3.one, rightHand.transform.localScale));
                Gizmos.color = new Color(1, 1, 1, 0.3f);
                Gizmos.DrawCube(leftHand.transform.position, Vector3.Scale(Vector3.one, leftHand.transform.localScale));
                break;
            case equipAreas.equipLeftHand:
                Gizmos.color = new Color(0, 1, 0, 0.3f); 
                Gizmos.DrawCube(leftHand.transform.position, Vector3.Scale(Vector3.one, leftHand.transform.localScale));
                Gizmos.color = new Color(1, 1, 1, 0.3f);
                Gizmos.DrawCube(rightHand.transform.position, Vector3.Scale(Vector3.one, rightHand.transform.localScale));
                break;
            case equipAreas.equipEitherHand:
                Gizmos.color = new Color(1, 1, 0, 0.3f);
                Gizmos.DrawCube(rightHand.transform.position, Vector3.Scale(Vector3.one, rightHand.transform.localScale));
                Gizmos.DrawCube(leftHand.transform.position, Vector3.Scale(Vector3.one, leftHand.transform.localScale));
                break;
            case equipAreas.equipNone:
                Gizmos.color = new Color(1, 1, 1, 0.3f);
                Gizmos.DrawCube(rightHand.transform.position, Vector3.Scale(Vector3.one, rightHand.transform.localScale));
                Gizmos.DrawCube(leftHand.transform.position, Vector3.Scale(Vector3.one, leftHand.transform.localScale));
                break;
            default:
                break;
        }
        
        
    }
   */
}
